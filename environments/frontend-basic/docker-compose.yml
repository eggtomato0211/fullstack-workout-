services:
  app:
    build: .
    ports:
      - "3000:5173"
    volumes:
      - ./workspace:/app
      - node_modules:/app/node_modules
    environment:
      - NODE_ENV=development

volumes:
  node_modules:

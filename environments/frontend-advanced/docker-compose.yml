services:
  app:
    build: .
    ports:
      - "3001:3000"
    volumes:
      - ./workspace:/app
      - node_modules:/app/node_modules
    environment:
      - NODE_ENV=development

volumes:
  node_modules:
